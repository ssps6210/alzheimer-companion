# ── 範本：複製成 .env 並填入你的金鑰 ──────────────────────
#   cp .env.example .env
# companion_web.py 啟動時會自動載入 .env（.env 已被 .gitignore，不會提交）。
# 值「不要」加引號：寫 NVIDIA_API_KEY=nvapi-xxx，不要寫 ="nvapi-xxx"。

# ── 大腦 LLM（必填）──────────────────────────────────────
# 預設用 NVIDIA Nemotron（到 build.nvidia.com 申請免費金鑰）。
# 要換別家？改 conf.yaml 的 llm.base_url / model / api_key_env（OpenAI 相容皆可），
# 再把金鑰放到這裡對應的變數名即可。
NVIDIA_API_KEY=nvapi-你的金鑰填這裡

# ── 家人通報 Telegram（選用，缺了就不通報）──────────────
# 到 @BotFather 開一個 bot 拿 token；chat id 用 @userinfobot 查。
# TELEGRAM_BOT_TOKEN=
# TELEGRAM_CHAT_ID=

# ── 語音克隆 Qwen（WSL2 端，qwen_tts_api.py）────────────────
# 這些是在「WSL2 的 shell」設，不是這個 .env；預設值寫在 qwen_tts_api.py。
# 換成你自己的參考音（也可直接用 /setup 前端上傳，不必碰這裡）：
#   export QWEN_REF=/mnt/d/你的/參考音.wav   # 需搭配同名 .txt 逐字稿（10-30 秒清楚人聲）
#   export QWEN_MODEL=Qwen/Qwen3-TTS-12Hz-1.7B-Base
#   export QWEN_PORT=50000
